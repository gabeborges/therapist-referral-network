Organize code by feature, not by type. Each feature gets its own directory with components, hooks, utils, and tests co-located.

Pattern:
- `features/<name>/components/`
- `features/<name>/hooks/`
- `features/<name>/__tests__/`
