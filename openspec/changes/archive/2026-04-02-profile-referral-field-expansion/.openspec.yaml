name: profile-referral-field-expansion
description: Expand therapist profile and referral form fields to match PT taxonomy, add autocomplete UI, and move option data from hardcoded constants to DB tables
status: ready
prd: openspec/prd-referral-network.md
artifacts:
  proposal:
    status: done
  design:
    status: done
    dependencies: [proposal]
  tasks:
    status: done
    dependencies: [design]
apply:
  requires: [tasks]
